SET(LIB_NAME libjson)

FILE(GLOB_RECURSE HEADER_LIST *.h*)
AUX_SOURCE_DIRECTORY(. SOURCE_LIST)

SOURCE_GROUP("libjson" FILES ${HEADER_LIST})
SOURCE_GROUP("libjson" FILES ${SOURCE_LIST})

ADD_LIBRARY(${LIB_NAME} ${HEADER_LIST} ${SOURCE_LIST})
TARGET_LINK_LIBRARIES (${LIB_NAME} json)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)
LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})

FOREACH(mylauncher
    cubes
    ssrr
    sssss
    ui
    sound
)
ADD_SUBDIRECTORY(${mylauncher})

SET_TARGET_ATTRIBUTE(${mylauncher} "sample")

ENDFOREACH()
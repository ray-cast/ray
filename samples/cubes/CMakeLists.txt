SET(LIB_NAME "cubes")

SET(HEADER_PATH ${INCLUDE_PATH}/${LIB_NAME})

FILE(GLOB HEADER_LIST *.h)
FILE(GLOB SOURCE_LIST *.cpp)

SOURCE_GROUP("cube" FILES ${HEADER_LIST})
SOURCE_GROUP("cube" FILES ${SOURCE_LIST})

ADD_EXECUTABLE(${LIB_NAME} ${HEADER_LIST} ${SOURCE_LIST})
TARGET_LINK_LIBRARIES(${LIB_NAME}
    ray
)
SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES VS_WINRT_EXTENSIONS FALSE)
SET_TARGET_PROPERTIES(${LIB_NAME} PROPERTIES VS_DEPLOYMENT_CONTENT FALSE)
cmake_minimum_required(VERSION 2.8)

LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)

FOREACH(tools
    FontGen
    HLSLcc
)
ADD_SUBDIRECTORY(${tools})

SET_TARGET_ATTRIBUTE(${tools} "tools")

ENDFOREACH()
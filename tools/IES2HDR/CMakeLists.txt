SET(LIB_NAME "IES2HDR")

SET(HEADER_PATH ${INCLUDE_PATH}/${LIB_NAME})

FILE(GLOB HEADER_LIST *.h)
FILE(GLOB SOURCE_LIST *.cpp)

SOURCE_GROUP("IES2HDR" FILES ${HEADER_LIST})
SOURCE_GROUP("IES2HDR" FILES ${SOURCE_LIST})

ADD_EXECUTABLE(${LIB_NAME} ${HEADER_LIST} ${SOURCE_LIST})

TARGET_LINK_LIBRARIES(${LIB_NAME} PRIVATE libimage)
TARGET_LINK_LIBRARIES(${LIB_NAME} PRIVATE libplatform)
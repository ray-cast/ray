SET(LIB_NAME "LightMapPack")

SET(HEADER_PATH ${INCLUDE_PATH}/${LIB_NAME})

FILE(GLOB HEADER_LIST *.h)
FILE(GLOB SOURCE_LIST *.cpp)

SOURCE_GROUP("LightMapPack" FILES ${HEADER_LIST})
SOURCE_GROUP("LightMapPack" FILES ${SOURCE_LIST})

ADD_EXECUTABLE(${LIB_NAME} ${HEADER_LIST} ${SOURCE_LIST})
TARGET_LINK_LIBRARIES(${LIB_NAME} libplatform libmodel)
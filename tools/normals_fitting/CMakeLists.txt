PROJECT(fitting)
SET(LIB_NAME "normals_fitting")

INCLUDE_DIRECTORIES(${INCLUDE_PATH})
INCLUDE_DIRECTORIES(./include)

FILE(GLOB SOURCE_LIST ./source/*.cpp)

SOURCE_GROUP("normals_fitting" FILES ${HEADER_LIST})
SOURCE_GROUP("normals_fitting" FILES ${SOURCE_LIST})

ADD_DEFINITIONS(-D_BUILD_DLL_EXPORT)
ADD_EXECUTABLE(${LIB_NAME} ${HEADER_LIST} ${SOURCE_LIST})